spring:
  application:
    name: Omni-BE-Sponsor

cors:
  urls:
    httpsGatewayURL: ${HTTPS_GATEWAY_URL}

springdoc:
  api-docs:
    path: ${SPONSOR_SWAGGER_PATH}
  swagger-ui:
    path: ${SPONSOR_SWAGGER_PATH}
